# 简化装箱评分系统方案 (v2.0)

针对反馈，保留外侧高度约束，并在排序优先级中增加对“刚好匹配的空间”的优先考量。本方案将彻底移除不确定性强的加权计算，改为严格的 **约束过滤 + 字典序优先级排序**。

## 改进方案核心逻辑

寻找最佳位置分为两个步骤：**硬约束过滤** 和 **四级严格优先级筛选**。

### 第一步：硬约束过滤 (Hard Constraints)
任何候选点必须**全部**满足以下硬约束，否则直接废弃（与原逻辑一致，保留了重要的业务规则）：
1. 碰撞检测，不能超过笼子总干涉高度。
2. 稳定性检测（至少60%底面支撑）。
3. **外侧高度约束**（确保不会在最外侧放置阻挡内侧的高大物体）。

### 第二步：多级字典序评估 (Lexicographical Sorting)
对所有合法的候选点，不使用乘法系数算总分，而是计算4个独立的特征，直接进行严格的主次排序比较。两位置相比，先比P1，打平比P2，依次推演：

**P1：最小化底部空隙 (Minimize Bottom Void) 【首要指标，越小越优】**
- **机制**：计算物体放置后其底面与下方已有高度图之间的空隙体积。
- **作用**：确保坚实的地基，防止“搭桥”产生永远无法利用的死区。

**P2：最大化四周贴合度/刚好匹配 (Maximize Side Adjacency) 【越高越优】**
- **机制**：如果物体掉进一个四面或三面都有比它高的货物（或笼壁）的“坑”里，贴合度就最高。我们统计物品的前、后、左、右四个侧面的有效贴合比例（范围 0~4）。
- **作用**：完美解决 **“优先放在刚好匹配的空间”**。如果有跟它长宽刚好的缝隙，它放进去后周边的贴合度会达到满值，就会在此阶段把其他空旷地带的候选点挤掉。

**P3：最小化放置最高点 (Minimize Max Z) 【越小越优】**
- **机制**：比较放置后该物体顶端的绝对高度。
- **作用**：保持表面平缓，抑制个别列突兀长高。

**P4：推向里左角落 (Minimize Distance to Back-Left Corner) 【越小越优】**
- **机制**：$D = (L - Y_{center})^2 + X_{center}^2$
- **作用**：在空隙、贴合度、高度都相似的情况下，让物品自动向最里面、最左边的空域挤压。

## 具体改动列表

### 1. [config.py] 清理冗余配置
- **保留**：笼体尺寸、 `OUTER_HEIGHT_TOLERANCE`、`OUTER_HEIGHT_CHECK_RATIO` 等硬约束配置。
- **删除**：所有 `WEIGHT_*`，彻底告别调参焦虑。

### 2. [packing_planner.py] 逻辑重构
- **保留并精简**：自适应放宽外侧高度约束的逻辑（只动阈值，不影响评分排序流程，保留笼满后的容错性）。保留天际线粗查+精查机制。
- **重构** `_evaluate_position`：
  在完成硬性约束检查后，返回一个用于排序的元组 tuple：
  `sort_key = (void_volume, -adjacency, z_max, corner_dist)`
  *(注意：贴合度 adjacency 取负号为了统一 Python 的升序排列 `a < b` 机制)*
- **删除** `_compute_score` 以及所有的权重动态计算逻辑，代码大幅缩减，运行速度增加。

## 预期效果
- **找缝隙能力拉满**：P2强制系统有缝就钻（不会钻进没底支撑的空洞，因为P1会拦住）。
- **零需要调优的评分超参数**：所有特征都自带直接的物理意义，系统不再因为某个系数设为3还是5而突然表现失常。
- **规则明确透明**：每次摆放都严格遵循这四条人类容易理解的准则。

请查看更新后的方案，如果没问题，我会立刻开始修改这两个文件。
