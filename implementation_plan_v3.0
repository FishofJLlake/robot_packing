# 提高在线装箱成功率与容积率

## 问题分析

当前系统的核心瓶颈：**固定权重的贪心策略**。不管笼子是空的还是快满了，评分权重不变，导致：
- 初期过于激进地追求"先里后外"，在里侧堆很高，外侧留大片空地
- 后期空间碎片化严重，大箱子放不下，小箱子才能塞进缝隙
- 约束过严导致很多位置被拒绝（尤其外侧高度约束在笼子快满时过于限制）

## 四项改进方案

### 1. 自适应权重策略（Adaptive Weights）

> 核心思想：根据当前装填状态动态调整权重，不同阶段有不同策略

| 阶段 | 填充率 | 策略重点 |
|------|-------|---------|
| 初期 (0~30%) | 低 | 强调先里后外、先下后上，建立良好底层 |
| 中期 (30~70%) | 中 | 平衡紧凑度和空间利用，开始重视gap-filling |
| 后期 (70%+) | 高 | 最大化紧凑度，强力避免空隙，不再执着于深度优先 |

实现：在 `_compute_score()` 中根据 `fill_ratio` 动态调整权重系数。

### 2. 可用空间追踪（Free Space Tracking）

> 核心思想：维护当前高度图中"最大可用矩形区域"列表，优先将物品放入匹配的空间

- 每次放置后，扫描高度图提取连续平坦区域
- 新物品到来时，先检查是否有"刚好匹配"的空间（Best-Fit）
- 避免把小物品放到大空间里，保留大空间给可能的大物品

### 3. 动态约束松弛（Dynamic Constraint Relaxation）

> 核心思想：笼子快满时，适度放松约束以容纳更多物品

| 约束 | 初期 | 后期（>70%高度） |
|------|------|----------------|
| 支撑面积比 | 60% | 逐步降至 40% |
| 外侧高度约束 | 5cm | 逐步放宽至 8cm |
| 违规列比例 | 67% | 逐步放宽至 80% |

### 4. 天际线候选生成（Skyline-based Candidates）

> 核心思想：不盲目遍历全网格，只在"天际线转折点"处评估候选

天际线 = 高度图中每一列的最大高度轮廓。在高度突变处放置物品最容易贴合，比全网格搜索更快更准。

## 改动文件

### [MODIFY] [config.py](file:///d:/Code/robot_packing/config.py)
- 新增自适应权重相关配置
- 新增动态约束松弛配置

### [MODIFY] [packing_planner.py](file:///d:/Code/robot_packing/packing_planner.py)
- 新增 `_get_adaptive_weights()` — 根据fill_ratio动态计算权重
- 新增 `_find_best_fit_regions()` — 可用空间匹配
- 修改 `_evaluate_position()` — 支持动态约束
- 修改 `_compute_score()` — 使用自适应权重
- 新增 `_extract_skyline_candidates()` — 天际线候选生成

## 预期效果

- 初期：与当前效果相似（稳定底层结构）
- 中后期：显著减少碎片化，成功放置更多物品
- 整体容积率提升 10~20%
