# Plus装箱策略及倾覆物理模拟实现

基于前面确定的实施计划，目前代码已经完全实现了增强的 `try_plus_packing`（Plus装箱策略）以及结合 MuJoCo 的落体物理引擎仿真。

## 实现概览

### 1. 配置 `config.py` 放宽要求
增加了新的超参数 `TRY_PLUS_PACKING` 和 `MIN_SUPPORT_LENGTH_RATIO_Y` 以控制容差。开启之后可以有效接受不完全支撑甚至是极端偏心的悬空放置。

### 2. 精调物理稳定性 `stability_checker.py`
当 Plus 装箱开启时：
* 屏蔽了原有的 $< 60\%$ 面积不足报错。
* 新增逻辑拦截严重“外侧悬空”导致物品必定掉出集装箱口的情形，其硬性要求支撑区的深度占物品深度的 $60\%$ 以上。
* 当系统测算出物体重心 (CoG) 从支撑边界脱离时，不再拒绝摆放，而是返回标志字段 `will_tilt = True`，让规划器进一步推演算物理倾覆形态。

### 3. MuJoCo物理模拟核心 `mujoco_simulator.py`
新增了一个基于无渲染运算级（`DIRECT`）的 MuJoCo 物理加速引擎绑定脚本。
* 系统将在物体的实时 `grid_pos` 附近建立一组密集高度方柱，它完美还原装箱里的 3D 几何（Heightmap）。
* 物体按基础姿态落下并进行最多5秒物理碰撞测试。
* 返回最终真实静止点，并排除滑落出货笼外的极端情形。

### 4. 数据融合与高阶规划 `packing_planner.py`
* 提取最优秀的合法位置尝试模拟。如果有候选位置发生倾覆，调用 `MujocoSimulator` 进行判断。
* 若成功稳住在容器内，将仿真后最终产生的四元数、位置及旋转矩阵记录入结果属性中（`simulated_pose`）。
* 在下一次物品规划生成（`update_heightmap_with_placement`）时，采用 `simulated_pose` 和 AABB 的粗略拓宽机制，填补由于物品倾斜带来向外拓展的占据空间，避免后续摆放物发生碰撞干涉。

### 5. 可视化保真适配
在 `visualize_demo.py` 和 `visualizer.py`（针对真实点云/Open3D）以及控制主文件 `main.py` 和 `test_packing.py` 中，增加了截取和应用 `simulated_pose` 的读取渲染功能，如果产生了偏斜放置，用户端图形化截屏中将实际显示物体立起来或者倾倒后的几何姿态，完全响应了真机测试中后续点云采样的结果！

> [!TIP]
> 目前代码已在 `test_packing.py` 和 `visualize_demo.py` 全部执行并模拟通过，新的仿真截图已自动保存在您的 `output/` 文件夹中。您可以查阅确认实际的跌落效果是否满足预期。
